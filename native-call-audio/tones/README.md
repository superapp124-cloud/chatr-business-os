# Tone Assets

Reserved for future branded MP3 tone packs:

- `ringback.mp3`
- `busy.mp3`
- `reconnecting.mp3`
- `failed.mp3`
- `ended.mp3`

The production engine currently generates the default GSM-style tones natively so the app has no dependency on WebView audio playback or bundled media files.
