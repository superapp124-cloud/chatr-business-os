{
  "abi": "chatr.capability_contracts.v1_0",
  "contract_version": "1.0.0",
  "description": "Universal Capability Contracts — CHATR Kernel v1.0. Defining rich capability profiles for the Universal Capability Platform.",
  "generated_at": "2026-07-20T00:00:00Z",
  "contracts": {
    "Knowledge.Discover": {
      "inputs": ["query", "context_ref"],
      "outputs": ["candidate_options"],
      "evidence": ["discovery_report"],
      "policies": ["privacy", "offline_first"],
      "security": "low",
      "timeout_ms": 5000,
      "retry": { "attempts": 2, "backoff": "exponential" },
      "compensation": "none",
      "idempotency": true,
      "concurrency": "parallel",
      "side_effects": false,
      "determinism": false,
      "required_permissions": ["read_knowledge"],
      "observability_level": "standard",
      "verification_policy": { "required": false }
    },
    "Knowledge.Store": {
      "inputs": ["artifact", "metadata"],
      "outputs": ["storage_receipt"],
      "evidence": ["storage_hash", "timestamp"],
      "policies": ["retention", "encryption"],
      "security": "medium",
      "timeout_ms": 10000,
      "retry": { "attempts": 3, "backoff": "linear" },
      "compensation": "delete_artifact",
      "idempotency": true,
      "concurrency": "sequential",
      "side_effects": true,
      "determinism": true,
      "required_permissions": ["write_knowledge"],
      "observability_level": "detailed",
      "verification_policy": { "required": true, "method": "hash_match" }
    },
    "Knowledge.Retrieve": {
      "inputs": ["artifact_id"],
      "outputs": ["artifact_content"],
      "evidence": ["access_log"],
      "policies": ["audit"],
      "security": "low",
      "timeout_ms": 2000,
      "retry": { "attempts": 3, "backoff": "immediate" },
      "compensation": "none",
      "idempotency": true,
      "concurrency": "parallel",
      "side_effects": false,
      "determinism": true,
      "required_permissions": ["read_knowledge"],
      "observability_level": "minimal",
      "verification_policy": { "required": false }
    },
    "System.Search": {
      "inputs": ["query", "scope"],
      "outputs": ["search_results"],
      "evidence": ["search_telemetry"],
      "policies": ["offline_first"],
      "security": "low",
      "timeout_ms": 3000,
      "retry": { "attempts": 1, "backoff": "none" },
      "compensation": "none",
      "idempotency": true,
      "concurrency": "parallel",
      "side_effects": false,
      "determinism": false,
      "required_permissions": ["read_system"],
      "observability_level": "minimal",
      "verification_policy": { "required": false }
    },
    "Planning.Schedule": {
      "inputs": ["time_range", "participants", "title"],
      "outputs": ["calendar_event_id"],
      "evidence": ["event_receipt"],
      "policies": ["conflict_resolution"],
      "security": "medium",
      "timeout_ms": 5000,
      "retry": { "attempts": 2, "backoff": "linear" },
      "compensation": "cancel_event",
      "idempotency": false,
      "concurrency": "sequential",
      "side_effects": true,
      "determinism": true,
      "required_permissions": ["write_calendar"],
      "observability_level": "standard",
      "verification_policy": { "required": true, "method": "read_back" }
    },
    "System.Persist": {
      "inputs": ["data_blob", "destination"],
      "outputs": ["file_path"],
      "evidence": ["file_hash", "fs_receipt"],
      "policies": ["fs_quota"],
      "security": "high",
      "timeout_ms": 15000,
      "retry": { "attempts": 2, "backoff": "linear" },
      "compensation": "delete_file",
      "idempotency": true,
      "concurrency": "sequential",
      "side_effects": true,
      "determinism": true,
      "required_permissions": ["write_filesystem"],
      "observability_level": "detailed",
      "verification_policy": { "required": true, "method": "stat_check" }
    }
  }
}
