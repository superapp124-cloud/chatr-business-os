{
  "import": {
    "supported_formats": ["csv", "json", "rest_api"],
    "mapping_templates": [
      {
        "name": "workday_candidate",
        "mapping": {
          "LegalName_First": "first_name",
          "LegalName_Last": "last_name",
          "EmailAddress": "email"
        }
      }
    ],
    "validation_rules": [
      { "field": "email", "rule": "is_valid_email" }
    ],
    "incremental": true
  },
  "export": {
    "supported_formats": ["csv", "json"],
    "incremental": true
  }
}
