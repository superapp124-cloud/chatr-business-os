{
  "id": "core.documents.read",
  "name": "core.documents.read",
  "version": "1.0.0",
  "description": "Reads and extracts the full text content of a local document given its file path.",
  "category": "utility",
  "inputs": {
    "type": "object",
    "properties": {
      "filePath": {
        "type": "string",
        "description": "The absolute path of the file to read (obtained from core.documents.search)"
      }
    },
    "required": ["filePath"]
  },
  "outputs": {
    "type": "object",
    "properties": {
      "text": {
        "type": "string",
        "description": "The parsed text content of the document"
      },
      "metadata": {
        "type": "object",
        "description": "Parsing metadata, warnings, or truncation status"
      }
    }
  },
  "ui": {
    "icon": "FileText",
    "color": "#10B981"
  }
}
