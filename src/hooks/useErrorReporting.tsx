// Stub file to satisfy cached imports - DO NOT USE
// This hook has been removed but cached JS still references it

export const useErrorReporting = () => {
 // No-op to prevent errors from cached code
 return null;
};
